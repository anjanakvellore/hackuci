import {TutorFilterPipe} from './tutor-filter.pipe';

describe('TutorFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new TutorFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
